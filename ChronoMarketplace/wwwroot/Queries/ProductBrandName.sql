SELECT PName, BrandName
FROM Products
JOIN Brands ON BrandId = BrandId;
/* The above SQL statement joins brand names from the brand table with product names from the product table into a single result. */